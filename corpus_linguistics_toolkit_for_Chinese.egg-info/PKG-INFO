Metadata-Version: 2.4
Name: corpus-linguistics-toolkit-for-Chinese
Version: 0.1.0
Summary: A Python toolkit for analysing Chinese corpora
Requires-Python: >=3.10
Description-Content-Type: text/markdown
Requires-Dist: requests
Requires-Dist: beautifulsoup4
Requires-Dist: opencc-python-reimplemented
Requires-Dist: selenium
Requires-Dist: matplotlib
Requires-Dist: jieba


## usage instructions
1. in your terminal, go to the direction where you have the files downloaded:
    ```
    cd D:\CL-Project
    ```
2. install the package
    ```
    pip install -e .
    ```
3. optional: show package information
    ```
     pip show corpus-linguistics-toolkit-for-Chinese
    ```
4. import the package
    ```
    python -c "import Toolkit; print('Toolkit imported successfully')"
    ```
    
## Sample commands


 1. preprocessing (POS tagging included); 
    optionally with document filtering based on metadata parameter(s):
    ```
    python -m Toolkit.preprocessing --license "CC BY-NC-SA 3.0" --genre "Online News Article"
    ```
    note: if your parameter has white space, put it in quotation marks "".
 
 2. frequency analysis
    ```
    python -m Toolkit.frequency
    ```

 3. corpus analysis
    ```
    python -m Toolkit.corpus_analysis
    ```
    By default, it shows only basic document info and TTR result.
    
    Use --kwic to include concordance analysis.
    ```
    python -m Toolkit.corpus_analysis --kwic 女性
    ```

 4. corpus search
 
 5. N-gram analysis
